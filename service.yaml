apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: testing
spec:
  selector:
    app: auth
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3001
  type: LoadBalancer
